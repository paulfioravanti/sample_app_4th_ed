<li>
  <%= user.gravatar_image(size: 50) %>
  <%= link_to user.name, user %>
  <% if current_user.can_delete?(user) %>
    | <%= link_to "delete",
                  user,
                  method: :delete,
                  data: { confirm: "You sure?" } %>
  <% end %>
</li>
