<div class="follow-form">
  <% if current_user.following?(user) %>
    <%= render "unfollow", followed_user: current_user.find_followed(user) %>
  <% else %>
    <%= render "follow",
               new_relationship: current_user.new_relationship,
               user_id: user.id %>
  <% end %>
</div>
