$(".follow-form").html(
  "<%=j render("follow",
               new_relationship: current_user.new_relationship,
               user_id: @user.id) %>"
);
$("#followers").html("<%= @user.followers.count %>");
